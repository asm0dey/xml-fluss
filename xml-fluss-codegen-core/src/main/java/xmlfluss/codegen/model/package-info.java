/**
 * Neutral DTOs describing a classified record: its components, sources, coercions,
 * and trie-friendly edge keys. Consumed by the {@code xmlfluss.codegen.classify}
 * package (Classifier) and by emitter-side bridges in each processor.
 */
@NullMarked
package xmlfluss.codegen.model;

import org.jspecify.annotations.NullMarked;
